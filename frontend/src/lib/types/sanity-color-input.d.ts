// types/sanity-color-input.d.ts
import type { Plugin } from 'sanity'

/**
 * Sanity’s color-input plugin.
 * Registers a `color` object type and its studio UI.
 */
export function colorInput(): Plugin
