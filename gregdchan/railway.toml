[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sirv dist --port ${PORT:-3333} --host 0.0.0.0 --single"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[service]
name = "sanity-studio"
